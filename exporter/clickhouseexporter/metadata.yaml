type: clickhouse
display_name: ClickHouse Exporter

status:
  class: exporter
  stability:
    development: [profiles]
    alpha: [metrics]
    beta: [traces, logs]
  distributions: [contrib]
  codeowners:
    active: [Frapschen, SpencerTorres]
    emeritus: [dmitryax, hanjm]

tests:
  config:
    endpoint: clickhouse://localhost:9000
